<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid>
        <Grid.Resources>
            <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" TargetType="Label" />
            <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}" TargetType="TextBox" />
            <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" TargetType="Button" />
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>

            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Button Grid.Column="2"
                Command="{Binding TestCommand}"
                CommandParameter="{Binding Name}"
                Content="Test"
                IsEnabled="{Binding HelloCommand.RunningCommandsCount.IsZero}" />

        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Margin="5" Content="IndentChars:" />
            <TextBox Grid.Column="1" Margin="5" VerticalContentAlignment="Center" Text="{Binding IndentCharsSetting}" />
            <Label Grid.Row="1" Margin="5" Content="NewLineOnAttributes:" />
            <CheckBox Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"
                      IsChecked="{Binding NewLineOnAttributesSetting}" />
            <Label Grid.Row="2" Margin="5" Content="AddEmptyLineBetweenGroups:" />
            <CheckBox Grid.Row="2" Grid.Column="1" Margin="5" VerticalAlignment="Center"
                      IsChecked="{Binding AddEmptyLineBetweenGroupsSetting}" />
        </Grid>
        <Grid Grid.Row="2" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Text}" />
            <Button HorizontalAlignment="Right" Command="{Binding SaveCommand}" Content="Save" />
        </Grid>

    </Grid>
</DataTemplate>