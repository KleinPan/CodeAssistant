<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
    <DataTemplate.Resources>
        <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" TargetType="Label" />
        <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.TextBoxStyleKey}}" TargetType="TextBox" />
        <Style BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ButtonStyleKey}}" TargetType="Button" />


        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource {x:Static styles:VsBrushes.BackgroundKey}}" />

        </Style>
        <Style TargetType="{x:Type TabItem}">

            <Setter Property="Background" Value="{DynamicResource {x:Static styles:VsBrushes.BackgroundKey}}" />

            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type TabItem}}}" Foreground="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" Text="{Binding}" />
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid SnapsToDevicePixels="true">
                            <Border x:Name="Border" Margin="0,0,2,0"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4,4,0,0">
                                <ContentPresenter x:Name="ContentSite"
                                                  Margin="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" ContentSource="Header" />
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static styles:VsBrushes.BackgroundKey}}" />
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource {x:Static styles:VsBrushes.ActiveBorderKey}}" />
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0" />

                                <Setter Property="Panel.ZIndex" Value="100" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource {x:Static styles:VsBrushes.BackgroundKey}}" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!--  定义选中状态的样式  -->
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True" />
                <Trigger Property="IsMouseOver" Value="True" />
            </Style.Triggers>
        </Style>
    </DataTemplate.Resources>
    <TabControl>
        <TabItem Header="FormatCSProj">
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>

                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <Button Grid.Column="2"
                        Command="{Binding TestCommand}"
                        CommandParameter="{Binding Name}"
                        Content="Test"
                        IsEnabled="{Binding HelloCommand.RunningCommandsCount.IsZero}" />

                <Grid Grid.Row="1" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="5*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Label Margin="5" Content="IndentChars:" />
                    <TextBox Grid.Column="1" Margin="5" VerticalContentAlignment="Center" Text="{Binding IndentCharsSetting}" />
                    <Label Grid.Row="1" Margin="5" Content="NewLineOnAttributes:" />
                    <CheckBox Grid.Row="1" Grid.Column="1" Margin="5" VerticalAlignment="Center"
                              IsChecked="{Binding NewLineOnAttributesSetting}" />
                    <Label Grid.Row="2" Margin="5" Content="AddEmptyLineBetweenGroups:" />
                    <CheckBox Grid.Row="2" Grid.Column="1" Margin="5" VerticalAlignment="Center"
                              IsChecked="{Binding AddEmptyLineBetweenGroupsSetting}" />
                </Grid>
                <Grid Grid.Row="2" Grid.ColumnSpan="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding Text}" />
                    <Button HorizontalAlignment="Right" Command="{Binding SaveCommand}" Content="Save" />
                </Grid>

            </Grid>
        </TabItem>
        <TabItem Header="FormatComment" />
    </TabControl>

</DataTemplate>